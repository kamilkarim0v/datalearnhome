# 🚀 Прогресс проекта "Data Engineer Journey"

Проект: ETL-пайплайн для загрузки биржевых данных из T-Invest API в PostgreSQL с оркестрацией Airflow.

---

## 📅 Хронология

### 14–15 июня — Первые шаги
- [x] Изучил API T-Invest, обошёл SDK через прямой REST
- [x] Написал первый скрипт (загрузка свечей в SQLite)
- [x] Освоил `requests`, `pandas`, `dotenv`

### 16–17 июня — Переход на PostgreSQL
- [x] Поднял PostgreSQL в Docker
- [x] Переписал загрузку в PostgreSQL
- [x] Добавил инкрементальную загрузку через временную таблицу
- [x] Добавил проверку дубликатов и логирование

### 18–19 июня — Рефакторинг и инфраструктура
- [x] Создал свой `docker-compose.yml` с PostgreSQL
- [x] Вынес переменные окружения в `.env`
- [x] Добавил `README.md`, структурировал проект

### 20–21 июня — ООП-рефакторинг
- [x] Разбил монолитный скрипт на классы:
  - `api_client.py` — работа с API
  - `db_manager.py` — работа с БД
  - `etl_pipeline.py` — оркестрация
- [x] Запустил ООП-пайплайн, данные загружаются

### 22–23 июня — Развёртывание Airflow
- [x] Развернул Airflow в Docker
- [x] Написал первый DAG (запуск ETL по расписанию)
- [x] Решил проблемы с переменными окружения и подключением к БД внутри контейнера

### 24–25 июня — Универсализация и интеграция
- [x] Интегрировал ETL с Airflow
- [x] Начал переписывать `db_manager` в универсальный инструмент
- [x] Обсудил архитектуру: разделение на слои (инфраструктура, бизнес-логика, оркестрация)

### 26–27 июня — Планирование и наброски
- [x] Определил финальную структуру проекта
- [x] Набросал методы для универсального `db_manager`
- [x] Спланировал реализацию ETL для активов (следующий шаг)

### 28 июня — Рефакторинг API-клиента и утилит
- [x] Перенёс `camel_to_snake` из `api_client` в отдельный модуль `utils/text.py`.
- [x] Настроил импорты и проверил работу в Jupyter.
- [x] Исправил URL в `BaseClient._post` (точка вместо слеша).
- [x] Данные из API успешно подтягиваются.

### 29 июня — Проектирование суперсырого слоя
- [x] Принял решение: загружать все ответы API в единую таблицу `extract_api_jobs` (суперсырой слой).
- [x] Определил поля таблицы: `request` (payload), `response` (JSON), `endpoint`, `status`, `loaded_at`, `id` (и возможно `figi`, `interval` для фильтрации).
- [x] Планирую партиционировать таблицу по `endpoint` или по дате загрузки.

### 📅 30 июня — Доработка API-клиента, суперсырой слой и логирование

- [x] Исправил URL в `BaseClient._post` (точка вместо слеша), API-запросы теперь работают.
- [x] Перенёс `camel_to_snake` в отдельный модуль `utils/text.py` (импорт работает).
- [x] Спроектировал таблицу `raw.extract_api_jobs` для суперсырого слоя (сохраняем все API-запросы как есть).
- [x] Решил не добавлять отдельные колонки (`figi`, `interval` и т.д.) — всё храним в `request_payload` (JSONB) для универсальности.
- [x] Начал реализацию `ApiJobLogger` (пока тестирую) — отдельного класса для логирования запросов в БД (использует `DatabaseManager`).
- [x] Протестировал в Jupyter: `get_candles` возвращает кортеж `(response, endpoint, payload)`, данные сохраняются в `raw.extract_api_jobs` (ошибка с колонкой `figi` устранена).

### 1 июля — Запуск DAG для сырого слоя, отладка и доработка

- [x] **Написал DAG `raw_candles_dag`** для загрузки сырых данных из API T-Invest в таблицу `raw.extract_api_jobs`.
- [x] **Интегрировал `ApiJobLogger`** — логирование запросов в БД.
- [x] **Обнаружил и исправил ошибку распаковки** `get_candles` (функция возвращает 4 значения, а не 3). Исправлено.
- [x] **Убедился, что DAG работает** — в БД появилась запись со `status_code = 200`. XCom показывает ошибку, но это баг интерфейса Airflow, данные сохранены корректно.
- [x] **Сравнил два подхода:** старый ETL (трансформация до загрузки) и новый ELT (сырой слой). Принял решение двигаться по пути ELT, так как он гибче и устойчивее к изменениям.
- [x] **Задавал вопросы про связку Airflow + dbt**, но решил пока отложить эту тему, чтобы сосредоточиться на базовом ELT-пайплайне.

### 2 июля — Изучение JSONB в PostgreSQL и планирование трансформаций

- [x] **Изучил работу с JSONB в PostgreSQL:** операторы `->`, `->>`, `#>`, `#>>`, функция `jsonb_array_length`.
- [x] **Освоил разворачивание массивов** с помощью `jsonb_array_elements` и `LATERAL`.
- [x] **Узнал, как строить индексы** на поля внутри JSONB для ускорения фильтрации.
- [x] **Научился подсчитывать количество элементов в массиве** (`jsonb_array_length`).
- [x] **Спланировал следующий шаг:** написать функцию `transform_candles`, которая будет читать сырые данные из `raw.extract_api_jobs` и загружать очищенные данные в `staging.candles`.
- [x] **Определил структуру таблицы `staging.candles`** (плоская, с колонками: `time`, `figi`, `interval`, `open`, `close`, `high`, `low`, `volume` и т.д.).

### 3 июля — Рефакторинг сервисов и начало работы над активами

- [x] **Переписал `MarketDataService`** — вынес `endpoint` в `__init__`, упростил структуру.
- [x] **Исправил ошибку** `AttributeError: 'MarketDataService' object has no attribute 'base_url'` — добавил вызов `super().__init__()` в конструктор, чтобы инициализировать атрибуты родительского класса `BaseClient`.
- [x] **Убедился, что DAG `raw_candles_dag` работает** — после исправления загрузка свечей в `raw.extract_api_jobs` проходит успешно.
- [x] **Начал писать `InstrumentService`** — класс для работы с методами `InstrumentsService` (акции, облигации, валюты и т.д.). Реализован метод `get_assets` с маппингом типов инструментов и статусов.
- [x] **Обсудил архитектуру сервисов** — каждый метод определяет свой `endpoint` (подход А), что делает код гибким и понятным.

### 4 июля — Работа над контролем пропусков и календарём

- [x] **Разработал структуру таблицы `dim_calendar`** для хранения торговых дней и дат.
- [x] **Написал SQL-запрос** для выявления пропущенных дат в загрузке свечей (сравнение с календарём).
- [x] **Изучил варианты интеграции проверки пропусков в Airflow** — через отдельную задачу в DAG или с использованием Airflow Variables.
- [x] **Протестировал запрос** на существующих данных в `raw.extract_api_jobs` — убедился, что логика работает корректно.

### 5 июля — Проектирование механизма дозагрузки исторических данных

- [x] Разработал структуру таблицы `interval_limits` для хранения лимитов API по интервалам свечей (1min, 5min, 15min, hour, day и др.).
- [x] Спроектировал SQL-запрос для автоматической генерации заданий на дозагрузку недостающих дат для новых связок (figi, interval) с учётом максимального количества свечей за запрос.
- [x] Определил подход к разбиению периода `2026-06-01..today` на блоки, не превышающие лимиты API (2400 свечей для большинства интервалов).
- [x] Начал интеграцию с календарём `dim_calendar` для учёта только торговых дней при генерации заданий.

### 6 июля — Создание таблицы лимитов и интеграция с запросом дозагрузки

- [x] Создал таблицу `raw.interval_limits` для хранения лимитов API по интервалам свечей (полное имя, короткое имя, max_limit, candles_per_day, max_days_calc).
- [x] Заполнил таблицу данными на основе документации T-Invest (все интервалы от 1min до month и секундные).
- [x] Исправил ошибку с типом данных `candles_per_day` (изменил `DECIMAL(10,6)` на `DECIMAL(12,6)` для поддержки значений >9999).
- [x] Интегрировал таблицу `interval_limits` в основной запрос на поиск недостающих дат — теперь для каждой пропущенной даты доступны `max_days_calc` и `interval_short`.
- [x] Спроектировал логику для DAG `backfill_candles`: получение списка недостающих дат → разбиение на блоки по `max_days_calc` → последовательная загрузка через `MarketDataService`.

### 7 июля — Проектирование backfill-архитектуры и доработка SQL-генерации блоков

- [x] Провёл анализ текущего подхода к определению недостающих дат — обнаружил, что группировка только по пропущенным дням приводит к превышению `max_days_calc` из-за пропусков между ними.
- [x] Переработал логику генерации блоков: теперь интервалы строятся по календарю с фиксированным шагом `max_days_calc`, а не по реальным датам загрузки. Это гарантирует, что каждый запрос к API не превышает лимит.
- [x] Написал финальный SQL-запрос для создания таблицы `backfill_candles_blocks` — блоки формируются от `2026-06-01` до вчерашнего дня с учётом `max_days_calc`, и в результат попадают только блоки, содержащие хотя бы одну недостающую дату.
- [x] Протестировал запрос на существующих данных — убедился, что `from_dt` и `to_dt` теперь соответствуют правильным диапазонам (например, 25 дней для 15min).
- [x] Спроектировал архитектуру DAG `backfill_candles`: чтение блоков → последовательная загрузка через `MarketDataService` → логирование в `raw.extract_api_jobs` с паузами между запросами.
- [x] Начал планировать интеграцию с `dim_calendar` и `interval_limits` в рамках единого процесса дозагрузки.

### 8 июля — Создание DAG для staging-слоя и проектирование таблицы

- [x] Начал разработку DAG `staging_candles_dag` для трансформации сырых данных из `raw.extract_api_jobs` в очищенный слой `staging.candles`.
- [x] Спроектировал структуру таблицы `staging.candles`:
  - Колонки: `datetime`, `figi`, `interval`, `open`, `close`, `high`, `low`, `is_complete`, `candle_source`, `volume`, `volume_buy`, `volume_sell`, `source_job_id`, `loaded_at`.
  - `PRIMARY KEY (datetime, figi, interval)` для гарантии уникальности.
  - Добавил служебные поля `source_job_id` (ссылка на raw) и `loaded_at` (время загрузки).
- [x] Написал функцию `init_db()` для создания схемы `staging` и таблицы `candles` (если не существуют).
- [x] Интегрировал `init_db()` как первую задачу в DAG (подготовка инфраструктуры перед трансформацией).

### 9 июля — Планирование архитектуры слоёв и интеграции dbt + Airflow

- [x] Обсудил назначение слоёв данных: `raw` (сырой), `staging` (очищенный/нормализованный), `mart` (витрины для аналитики).
- [x] Определил, что текущая задача — перенести трансформации из `raw` → `staging` в dbt, а не писать их в Python внутри Airflow.
- [x] Изучил варианты интеграции dbt и Airflow:
  - `BashOperator` — простой, но нет видимости отдельных моделей.
  - Специализированные операторы (`airflow-dbt-python`, `dbt-af`) — лучше, но требуют настройки.
  - **Astronomer Cosmos** — рекомендованный подход: каждая dbt-модель становится отдельной таской в Airflow, с логированием и зависимостями.
- [x] Принял решение использовать **Cosmos** для автоматического построения DAG по dbt-проекту.
- [x] Спроектировал структуру dbt-модели для `staging.candles` (разворачивание JSONB из `raw.extract_api_jobs` в плоскую таблицу).
- [x] Определил последовательность пайплайна:
  1. `raw_candles_dag` (загрузка в raw)
  2. **dbt-проект** (трансформации staging → mart), запускаемый через Cosmos
  3. (в будущем) `backfill_candles` как часть dbt или отдельный DAG

### 10 июля — Настройка dbt + Cosmos в Airflow

- [x] Создал и настроил Dockerfile.airflow с установкой `astronomer-cosmos`, `dbt-core`, `dbt-postgres`.
- [x] Пересобрал образы Airflow (успешно, без ошибок).
- [x] Написал DAG `dbt_staging_dag` через Cosmos, который автоматически запускает dbt-модели.
- [x] Устранил ошибку подключения к БД в Cosmos — проверил и исправил Connection `postgres_default` в Airflow.
- [x] Запустил DAG — dbt выполнил модель `stg_candles`, создал представление (view) в схеме `public`.
- [x] Обнаружил, что dbt создал схему `public_staging` вместо `staging` — проблема в несовпадении имени проекта в `dbt_project.yml`.
- [x] Заметил, что в UI появились два DAG: ручной `staging_candles_dag` и `dbt_staging_dag` (созданный Cosmos).
- [ ] Исправить `dbt_project.yml` (заменить `my_dbt_project` на `datalearnhome`).
- [ ] Проверить модель `stg_candles.sql` — убрать лишнюю конфигурацию схемы.
- [ ] Перезапустить DAG, чтобы таблица создалась в схеме `staging`.
- [ ] Решить, оставить ли ручной DAG `staging_candles_dag` или отключить.

### 11 июля — Архитектурная рефлексия, анализ текущего состояния

- [x] Провёл мысленный анализ текущей архитектуры проекта:
  - Слои: `raw` → `staging` (через dbt) → планируется `mart`.
  - Интеграция dbt + Cosmos в Airflow — работает, но требует донастройки (схема `staging`).
  - Два DAG: ручной `staging_candles_dag` и автоматический `dbt_staging_dag` — нужно решить, какой оставить.
- [x] Обдумал дальнейшие шаги:
  - Исправить `dbt_project.yml` для корректной материализации в схеме `staging`.
  - Настроить отказоустойчивость и логирование в Cosmos.
  - Подумать о добавлении витрин (`mart`) и автоматизации backfill.
- [x] Отметил, что следующий рабочий день начнётся с исправления `dbt_project.yml` и проверки создания таблицы в нужной схеме.

### 13 июля — Устранение конфликта схем в dbt и настройка корректной материализации

- [x] Обнаружил и исправил ошибку `Runtime Error: 'schema' is a required property` в dbt — ошибка возникала из-за отсутствия явной схемы в `ProfileConfig`.
- [x] Добавил в `ProfileConfig` параметр `profile_args={"schema": "staging"}` — это позволило dbt запускаться без ошибок.
- [x] Выявил проблему дублирования схемы: при наличии `+schema: staging` в `dbt_project.yml` и `profile_args={"schema": "staging"}` в профиле, dbt создавал таблицу в схеме `staging_staging`.
- [x] Убрал `+schema: staging` из `dbt_project.yml`, оставив только схему в `ProfileConfig` — теперь dbt создаёт таблицу в корректной схеме `staging`.
- [x] Убедился, что DAG `dbt_staging_dag` отрабатывает успешно — таблица `staging.candles` создаётся в правильной схеме.

### 13 июля — Обсуждение архитектуры инкрементальной загрузки

- [x] Настроил `RenderConfig(select=["staging"])` в DAG `staging_candles_dag.py` — теперь в Airflow выполняются только модели из папки `staging`.
- [x] Провёл детальное обсуждение архитектуры инкрементальной загрузки:
  - разделение на три модели (extract → temp → final)
  - добавление колонки `min_candle_time` в `raw.extract_api_jobs`
  - фильтрация на уровне raw-таблицы до разворачивания JSON
  - использование `MAX(datetime)` из `staging.candles` для инкремента
  - добавление PRIMARY KEY через `post_hook`
- [x] Сформировал собственное целостное видение реализации, которое будет применено в коде (твой подход, не мой).

### 14 июля — Устранение ошибок в пайплайне и планирование дальнейшей архитектуры

- [x] Обнаружил и исправил ошибку при попытке выполнить DROP TABLE IF EXISTS stg_candles — объект оказался представлением (VIEW).
- [x] Заменил команду на DROP VIEW IF EXISTS stg_candles; для корректного удаления.
- [x] Провёл анализ подзапроса для инкрементальной фильтрации в temp_stg_candles — убедился, что обращение к staging.candles корректно, если таблица существует, и обработал случай отсутствия таблицы через COALESCE.
- [x] Обсудил архитектуру трёхуровневых моделей dbt (extract → temp → final) и необходимость добавления колонки min_candle_time в raw.extract_api_jobs для оптимизации чтения.
- [x] Спланировал переход к использованию локального Data Lakehouse (Trino, Spark, MinIO, Iceberg) в рамках дальнейшего развития проекта.

### 15 июля — Изучение PySpark в командировке

· Начал знакомство с PySpark: посмотрел несколько уроков по основам, работе с DataFrames и трансформациям.
· Продолжаю вести челлендж 100 дней коммитов, несмотря на командировку — фиксирую мысли и прогресс в заметках.

### 16 июля 2026 — Командировка, день 3

- [x] Продолжил изучение PySpark (DataFrame API, трансформации, lazy evaluation).
- [x] Обдумывал, как интегрировать PySpark в существующий ETL-пайплайн для обработки больших объёмов данных.
- [x] Наметил архитектуру: PySpark как альтернатива dbt для тяжёлых трансформаций (или как дополнительный слой).

### 17 июля — Обдумывание архитектуры и планов на развитие проекта

- [x] Обдумал последовательность внедрения новых инструментов: Spark → S3 + Iceberg → Trino.
- [x] Определил, что Spark будет внедряться первым (как must-have навык для работы с большими данными).
- [x] Сформировал план из трёх фаз:
  1. Завершить текущий пайплайн (Airflow + dbt + PostgreSQL).
  2. Интегрировать PySpark для трансформаций (замена/дополнение dbt).
  3. Перейти на S3 (MinIO) + Parquet/Iceberg, затем добавить Trino для аналитики.
- [x] Закрепил понимание, что dbt останется для витрин (mart), а Spark займёт тяжёлые трансформации.
- [x] Отметил, что после возвращения из командировки нужно завершить три модели `stg_candles` и только потом переходить к Spark.

### 18 июля — Осмысление пройденного пути и планирование следующего шага

- [x] Сформулировал главный инсайт: пайплайн можно строить по-разному, выбор инструментов зависит от задачи, а не от моды.
- [x] Осознал, что за месяц попробовал Python-модули, Docker, Airflow, dbt, логирование, переменные окружения — это дало системное понимание архитектуры DE.
- [x] Определил, что в ближайшее время хочу собрать минимальный рабочий пайплайн на SQL + Airflow, а затем добавлять инструменты по мере необходимости.
- [x] Принял решение: не пытаться охватить всё сразу, а двигаться итеративно, с фокусом на конечный результат.

### 19 июля — Размышления об отказе от PostgreSQL и переходе на Parquet

- [x] Обдумал возможность не разворачивать PostgreSQL в новом проекте, а сразу использовать Parquet как основной формат хранения.
- [x] Выяснил, что JSON можно хранить в Parquet несколькими способами: как встроенный логический тип JSON, как строку или как развёрнутую структуру (struct).
- [x] Сформировал понимание, что Parquet лучше подходит для аналитических пайплайнов, а JSONB из PostgreSQL — для точечных запросов.
- [x] Принял решение: в следующей итерации проекта попробовать хранить сырые данные (ответы API) в Parquet и обрабатывать их через Spark, отказавшись от PostgreSQL как основного хранилища.
- [x] Отметил, что такой подход открывает путь к S3, Iceberg, Trino и современному Data Lakehouse без привязки к одной БД.

### 20-25 июля - on vocation

### 26 июля — проверка проекта «как пользователь» на Windows
- [x] Взял проект в руки и попробовал запустить как обычный пользователь: поднял стек на ноутбуке с голой Windows (без WSL и Linux).
Сразу поймал проблему с правами в Airflow: при попытке запуска вылезла знакомая ошибка PermissionError на папке логов — Docker на Windows тоже спотыкается о несоответствие UID.
- [x] Быстро пофиксил без лишних настроек: закомментировал volume для логов (./airflow/logs:/opt/airflow/logs) в docker-compose.yml. Это сразу убрало проблему с правами и позволило стеку подняться.
- [x] Убедился, что пайплайн остаётся рабочим: логи задач по‑прежнему доступны в UI Airflow, а для отладки можно смотреть через docker compose logs.
- [x] Актуализировал progress.md и зафиксировал паузу: добавил записи за дни отпуска — челлендж 100 дней продолжается, даже если были перерывы.
- [x] План на завтра: запустить DAG, проверить, что сырые данные пишутся в raw.extract_api_jobs, и убедиться, что dbt-модели отрабатывают корректно.

### 07.26 - 08.03 - on vocation

### 08.03 — Синхронизация проекта и восстановление рабочего окружения
- [x] Синхронизировал локальный репозиторий с удалённым: разрешил расхождение веток (pull --rebase), обновил все файлы (включая docker-compose.yml и progress.md).
- [x] Проверил, что стек (Docker, Airflow, dbt) поднимается без ошибок на основном ПК.
- [x] Бегло просмотрел актуальные DAG'и (raw_candles_dag, dbt_staging_dag) — общее состояние оценил как рабочее.
- [x] Зафиксировал намерение: на ближайшие дни — плавное вхождение в ритм, без форсирования, с фокусом на поддержку ежедневной привычки.

### 08.04 — Изучение теоретических материалов по dbt и Apache Iceberg
- [x] Посмотрел вводное видео по dbt: узнал о возможности разбивать проект на слои, описывать каждую модель и вести data catalog, который можно хранить в Git.
- [x] Изучил доклад по Apache Iceberg: понял принципы работы формата, его роль в современных Data Lake архитектурах, а также отличие copy-on-write от merge-on-read.
- [x] Сформировал понимание, что Iceberg позволяет строить ACID-таблицы в объектных хранилищах (S3) без дорогих перезаписей данных.

### 08.05 — Исправление SSL-ошибки и проверка работоспособности DAG
- [x] Обнаружил ошибку `SSLCertVerificationError` при обращении к T-Invest API из контейнера Airflow.
- [x] Временно отключил проверку SSL-сертификата в `api_client.py` (параметр `verify=False` для `requests.post`).
- [x] После исправления DAG `raw_candles_dag` успешно отработал: данные загрузились в `raw.extract_api_jobs`.
- [x] Проверил, что логи читаются нормально (том для логов размонтирован не был, права настроены через `AIRFLOW_UID`).

### 08.06 — Осознанная пауза и смена фокуса обучения
- [x] Принял решение замедлиться и осмыслить пройденный путь после отпуска.
- [x] Сместил фокус с активной разработки на просмотр обучающего контента: краткий обзор по терминалу Linux и лекция по высоконагруженным системам (1/3 книги).
- [x] Понял, что быстрый контент не всегда усваивается, и сформулировал цель на следующую неделю: восстановить рабочий ритм через небольшой, но законченный шаг, а не через попытку "перебороть" себя.
- [x] Планирую на выходных описать текущее видение проекта в файле `PROJECT_REVIVAL.md` (или обновить `PROGRESS.md`), чтобы структурировать мысли перед следующим этапом работы.

### 08.07 — Завершение темы высоконагруженных систем и переход к Data Modeling

- [x] Досмотрел обзорное видео/лекцию по высоконагруженным системам (третья часть) — закрепил понимание масштабирования и архитектурных подходов.
- [x] Начал поиск и изучение материалов по **Data Modeling** (моделирование данных) — следующая тема в моём образовательном треке.
- [x] Продолжаю поддерживать учебный ритм через смену формата: вместо кода — теория и архитектурные концепции (осознанная пауза перед следующим шагом).

### 08.08 — Настройка окружения на ноутбуке, аудит документации и борьба с SSL

- [x] Работал с ноутбука: синхронизировал проект через `git pull`, обновил локальные файлы.
- [x] Настроил `AIRFLOW_UID` в `.env`: узнал свой UID через `id -u`, прописал в `.env` и перезапустил стек — Airflow поднялся.
- [x] Создал новый токен T-Invest для ноутбука, попробовал запустить DAG.
- [x] Обнаружил ошибку SSL при запросе к API (самоподписанный сертификат в цепочке).
- [x] Добавил в `Dockerfile.airflow` установку `ca-certificates` и обновление сертификатов, пересобрал образ.
- [x] Провёл аудит `README.md`: обнаружил отсутствие инструкций по:
  - Настройке `AIRFLOW_UID` в `.env`.
  - Получению токена в T-Invest.
  - Ссылке на документацию API.
- [x] Заметил, что в проекте нет `requirements.txt` — задача на завтра.
- [x] После пересборки образа SSL-ошибка сохранилась — завтра разбираться с ручным импортом сертификатов или настройкой `verify=False` в коде.
- [x] Начал чтение книги «Высоконагруженные системы» (вне проекта, для общего развития).

### 08.09 — Погружение в Data Modeling: концептуальная, логическая и физическая модели

- [x] Изучил три уровня моделирования данных:
  - **Концептуальная модель** — бизнес-уровень: определяет, какие данные важны для бизнеса, какие источники критичны, а от каких можно отказаться на ранних этапах. Требует глубокого погружения в бизнес-процессы.
  - **Логическая модель** — архитектурный уровень: выбор формата хранения (Data Vault, Dimension Modeling, Big Table и др.). Определяет структуру, но не привязана к конкретной СУБД.
  - **Физическая модель** — технический уровень: создание таблиц, схем, индексов, ключей, распределений (шардирование/партиционирование) с учётом конкретной базы данных.
- [x] Закрепил понимание, что логическая модель не обязана полностью совпадать с физической — это два разных слоя абстракции.
- [x] Отметил, что в текущем проекте уже используются элементы всех трёх уровней (сырые слои → staging → витрины), но нужно осмыслить это более системно.

### 08.10 — Изучение сенсоров Airflow и кросс-DAG взаимодействий

- [x] Посмотрел видео по Airflow: разобрал, как работают сенсоры (`ExternalTaskSensor`, `FileSensor`, `TimeSensor` и др.).
- [x] Понял механизм взаимодействия между DAG’ами через `ExternalTaskSensor` (ожидание завершения задачи/дага в другом DAG).
- [x] Осознал, что сенсоры позволяют строить сложные зависимости между пайплайнами, не смешивая их в одном DAG.
- [x] Отметил, что в текущем проекте можно применить `ExternalTaskSensor` для ожидания загрузки `raw_candles_dag` перед запуском `dbt_staging_dag`.

### 08.11 — Погружение в продвинутый Airflow: CeleryExecutor, Redis, архитектура распределённых систем

- [x] Прочитал и разобрал статью по масштабированию Airflow с CeleryExecutor и Redis.
- [x] Закрепил понимание:
  - **Redis** — in-memory брокер сообщений, связывает Scheduler и Workers.
  - **Celery Workers** — исполнители задач, могут быть запущены на разных машинах (горизонтальное масштабирование).
  - **Очереди (Queues)** — позволяют направлять задачи на конкретные воркеры (например, мощные для тяжёлых задач).
  - **Flower** — инструмент мониторинга воркеров (порт 5555).
- [x] Осознал, что CeleryExecutor — следующий шаг для продакшен-архитектуры, и что это можно развернуть локально через Docker Compose.
- [x] Создал `requirements.txt` для проекта: отфильтровал нужные пакеты, убрал лишние зависимости (Jupyter, IPython, gRPC и т.д.).
- [x] Настроил трекер занятий (вне проекта) для фиксации ежедневного прогресса по темам.

### 08.12 — Планирование углублённого изучения Airflow и настройка окружения на ноутбуке

- [x] Синхронизировал репозиторий на ноутбуке через `git pull --rebase origin master`.
- [x] Создал и активировал виртуальное окружение, установил зависимости из `requirements.txt`.
- [x] Обнаружил проблему с установкой `t-tech-investments` (пакет недоступен в PyPI) — решил установить его отдельно через специальный индекс-адрес.
- [x] Разработал структурированный план углублённого изучения Airflow (6 модулей, от базовых концепций до продвинутых практик).
- [x] Дополнил план темами, важными для продакшена и собеседований: отладка DAG, обработка ошибок, кастомные хуки, динамические задачи, антипаттерны.
- [x] Осознал, что следующий шаг — перейти от теории к практике: внедрить изученные концепции в свой проект (TaskFlow API, сенсоры, XCom).

### 08.13 — Начало изучения Apache Airflow: роль в дата-пайплайнах

- [x] Приступил к углублённому изучению Airflow согласно составленному плану.
- [x] Разобрал первую тему: роль Airflow в построении современных дата-пайплайнов.
  - Airflow — оркестратор, а не исполнитель: управляет временем, порядком и отказоустойчивостью.
  - Используется для координации задач между различными системами (базы данных, Spark, API).
  - Обеспечивает наблюдаемость, перезапуски и стандартизацию пайплайнов.
- [x] Связал теорию с собственным проектом: существующие DAG'и (`raw_candles_dag`, `dbt_staging_dag`) — примеры оркестрации через Airflow.
- [x] Закрепил понимание, что Airflow — это «дирижёр», который управляет пайплайном, не выполняя тяжёлые вычисления сам.

### 08.14 — Обзор Apache Kafka: ключевые концепции и применение в DE

- [x] Посмотрел обзорное видео по Kafka для дата-инженеров.
- [x] Разобрал основные компоненты:
  - **Топик** — хранилище сообщений.
  - **Продюсер** — отправляет события.
  - **Консюмер** — читает данные, группа консюмеров управляет офсетами.
  - **Офсет** — порядковый номер сообщения для контроля чтения.
  - **Zookeeper** (и KRaft) — координация кластера.
- [x] Понял ключевое свойство Kafka: **write once, read many** — данные записываются один раз и могут читаться множеством консюмеров.
- [x] Узнал о практических сценариях:
  - Real‑time аналитика и event‑driven архитектура.
  - Обнаружение фрода, анализ поведения пользователей.
  - Тестирование новых фич.
- [x] Рассмотрел варианты сохранения данных из Kafka: в S3 (Parquet) через Python и в ClickHouse через материализованные представления.
- [x] Отметил рекомендации: всегда использовать группы консюмеров, структурированные форматы (JSON/Avro), партиционирование для производительности.
- [x] Зафиксировал, что Kafka — следующий шаг в развитии DE-навыков после Airflow.

### 08.15 — Размышления о пути инженера: от пользователя к создателю

- [x] Посмотрел лекцию по Apache Iceberg + Trino: разобрал концепции снапшотов, атомарных коммитов, метаданных и принципов работы Iceberg в дата-лейках.
- [x] Осознал, что изучение внутреннего устройства инструментов (Iceberg, Airflow, Kafka) — это переход на уровень Senior/Staff DE, когда ты понимаешь не только как использовать, но и как устроена система.
- [x] Обсуждал вопросы профессионального развития: сравнение пути разработчика и дата-инженера, потолки в карьере, философия «IT как путь самурая».
- [x] Сформулировал личный подход к разработке: исследование в Jupyter → рефакторинг в чистую архитектуру → интеграция в пайплайн. Закрепил понимание, что это и есть инженерный цикл.
- [x] Принял решение продолжать развиваться в DE, углубляясь в инструменты и архитектуру, а не распыляться на изучение всего подряд.

### 08.16 — Углубление в метаданные PostgreSQL и отладка dbt + Cosmos

- [x] Исправил ошибку `AirflowNotFoundException: The conn_id 'postgres_default' isn't defined` — причина была в отсутствии Connection в Airflow UI после пересборки окружения.
- [x] Восстановил подключение `postgres_default` в Airflow (Host: `db`, Schema: `staging`, Login/Password из `.env`).
- [x] Изучил работу с метаданными PostgreSQL:
  - Основные запросы в `information_schema` (таблицы, колонки, первичные ключи, схемы).
  - Детальные запросы в `pg_catalog` (размер таблиц, оценка количества строк, статистика анализа).
  - Применил на практике: написал запросы для проверки структуры `staging.candles` и `raw.extract_api_jobs`.
- [x] Закрепил понимание, что `conn_id` критичен для работы хуков, сенсоров и Cosmos — и его наличие/отсутствие напрямую влияет на выполнение DAG’ов.
- [x] Зафиксировал полезные SQL-шаблоны для автоматических проверок (существование таблицы, схема колонок) — пригодятся для тестирования данных.

### 08.17 — Отладка dbt-моделей, управление схемами и PRIMARY KEY

- [x] Обнаружил проблему: `post_hook` с `ALTER TABLE ADD PRIMARY KEY` падал из-за того, что таблица ещё не существовала на момент выполнения.
- [x] Решил: создал таблицу `staging.candles` вручную через DBeaver с PRIMARY KEY, затем убрал `post_hook` из dbt-модели.
- [x] Столкнулся с дублированием схемы: dbt создавал таблицу в `staging_staging.candles` из-за конфликта `+schema: staging` в `dbt_project.yml` и `profile_args={"schema": "staging"}` в DAG.
- [x] Исправил: убрал `+schema: staging` из `dbt_project.yml`, оставив только в `ProfileConfig`.
- [x] Переименовал модель `stg_candles.sql` → `candles.sql`, но dbt не видел новое имя из-за кеша в `target/partial_parse.msgpack`.
- [x] Очистил кеш dbt через `dbt clean`, после чего модель стала видна.
- [x] Убедился, что данные загружаются в правильную схему (`staging.candles`), а DAG `dbt_staging_dag` отрабатывает зелёным.

### 08.18 — Очистка кеша dbt и восстановление работы модели

- [x] Обнаружил проблему с кешированием в Cosmos: после переименования модели `stg_candles.sql` → `candles.sql` DAG продолжал искать старое имя из-за файла `partial_parse.msgpack`.
- [x] Очистил кеш dbt через удаление папки `target/` внутри контейнера Airflow (`/opt/airflow/dbt`).
- [x] После очистки модель `candles` успешно распознаётся и выполняется в DAG `dbt_staging_dag`.

### 08.19 — Возврат к основам Python и продолжение отладки dbt

- [x] Начал просмотр курса по Python для систематизации фундаментальных знаний.
- [x] Продолжил разбираться с проблемой кеширования в Cosmos: модель `stg_candles` не обновляется на `candles` из-за кеша в `/tmp/cosmos/` и отсутствия `profiles.yml` в контейнере.
- [x] Выяснил, что папка `.dbt` не нужна для работы Cosmos — он использует `ProfileConfig` из DAG.
- [x] Удалил кеш Cosmos (`rm -rf /tmp/cosmos/`) и перезапустил Airflow для принудительного обновления списка моделей.

### 08.20 — Практика на Leetcode и закрепление Python

- [x] Начал регулярную практику на Leetcode: решил 3 задачи на Python.
- [x] Разобрал и реализовал задачи на:
  - подсчёт пар с разностью `k` (с использованием `Counter` и однопроходного алгоритма со словарём);
  - группировку анаграмм (с использованием `defaultdict` и сортировки строк).
- [x] Закрепил понимание работы `Counter`, `defaultdict`, `.get()` и `dict.values()`.
- [x] Осознал, что на собеседованиях можно использовать `collections` — это стандартная библиотека Python, и важно уметь объяснить, как она работает под капотом.

### 08.21 — Решение задач на Leetcode: закрепление алгоритмов

- [x] Решил 3 задачи на Leetcode:
  - **Reverse Vowels** — разворот гласных в строке с использованием двух указателей.
  - **Valid Palindrome** — проверка палиндрома с пропуском небуквенно-цифровых символов.
  - **First Palindrome** — поиск первого палиндрома в списке строк.
- [x] Закрепил навыки работы с:
  - двухуказательной техникой,
  - методами `isalnum()`, `lower()`,
  - разделением логики на вспомогательные функции.
- [x] Осознал, что Leetcode — хороший инструмент для тренировки алгоритмического мышления, но для DE важнее практика в проекте (Airflow, dbt, SQL, Docker).

### 08.22 — Решение задач на Leetcode: закрепление техники двух указателей

- [x] Решил 4 задачи на Leetcode:
  - **Move Zeroes** — перемещение нулей в конец массива in‑place с сохранением порядка ненулевых элементов.
  - **Remove Duplicates from Sorted Array** — удаление дубликатов из отсортированного массива с возвратом новой длины.
  - **Squares of a Sorted Array** — построение массива квадратов в отсортированном порядке с использованием двух указателей.
  - **Two Sum II — Input Array Is Sorted** — поиск пары с заданной суммой в отсортированном массиве.
- [x] Закрепил технику двух указателей:
  - сравнение квадратов с краёв,
  - перемещение нулей,
  - удаление дубликатов,
  - сдвиг указателей в зависимости от суммы.
- [x] Осознал, что понимание задачи часто приходит не сразу, но ключевой навык — умение разбивать её на шаги и тестировать на примерах.

### 08.23 — Решение задач на Leetcode: палиндромы, бинарный поиск и реверс строк

- [x] Решил 3 задачи на Leetcode:
  - **Longest Palindrome** — поиск самой длинной палиндромной подстроки с использованием расширения от центра.
  - **Binary Search** — классическая реализация бинарного поиска с защитой от переполнения (`mid = l + (r - l) // 2`).
  - **Reverse String II** — реверс первых `k` символов в каждой группе из `2k` символов с использованием двух указателей.
- [x] Закрепил технику расширения от центра для палиндромов, обработку чётных и нечетных центров.
- [x] Осознал важность деталей: защита от переполнения при вычислении `mid`, работа с границами строки в цикле.
- [x] Продолжаю вести трекер сессий (дата, длительность, количество задач, комментарии) — помогает видеть прогресс и возвращаться к сложным темам.

### 08.24 — Решение задач на Leetcode: бинарный поиск и поиск границы

- [x] Решил 2 задачи на Leetcode:
  - **Guess Number Higher or Lower** — классический бинарный поиск с использованием внешней функции `guess()`.
  - **First Bad Version** — поиск первой версии, где условие становится истинным (поиск границы).
- [x] Закрепил важный паттерн бинарного поиска: для поиска первой позиции, где условие переходит из `False` в `True`, ответом после цикла является `l`.
- [x] Оптимизировал вызов `guess(mid)` — сохраняю результат в переменную, чтобы не вызывать дважды.
- [x] Осознал разницу между поиском значения и поиском границы.

### 08.25 — Решение задачи на Leetcode: поиск позиции вставки

- [x] Решил 1 задачу на Leetcode:
  - **Search Insert Position** — поиск индекса для вставки элемента в отсортированном массиве с использованием бинарного поиска.
- [x] Закрепил паттерн бинарного поиска для нахождения границы: `return l` после завершения цикла.
- [x] Осознал разницу между поиском значения и поиском позиции вставки в отсортированном массиве.

### 08.26 — Решение задач на Leetcode: бинарный поиск и поиск границ

- [x] Решил 2 задачи на Leetcode:
  - **Valid Perfect Square** — проверка, является ли число полным квадратом, с использованием бинарного поиска (оптимизация: `r = num // 2 + 1` для `num > 1`).
  - **Maximum Count of Positive and Negative Integers** — поиск границ отрицательных и положительных чисел в отсортированном массиве с помощью двух бинарных поисков.
- [x] Закрепил паттерны бинарного поиска:
  - для поиска квадратного корня,
  - для поиска границ (первое неотрицательное, первое положительное).
- [x] Оптимизировал вычисления: сохранение `mid * mid` в переменную, защита от переполнения.

### 08.27 — Стратегическое решение: углублённое изучение Python

- [x] Принял решение сфокусироваться на фундаментальном освоении Python до уровня, достаточного для уверенного прохождения технических собеседований и работы Middle DE.
- [x] Основные направления:
  - алгоритмы и структуры данных (словари, множества, списки, очереди, деревья),
  - объектно-ориентированное программирование (ООП),
  - принципы SOLID,
  - работа с коллекциями, итераторами и генераторами.
- [x] Осознал, что глубокая база в Python — это фундамент, на котором строятся все остальные инструменты (Airflow, dbt, Spark, API-клиенты).
- [x] Планирую уделять этому направлению основное время в ближайшие недели, продолжая решать задачи на Leetcode и внедряя изученные паттерны в пет-проект.