name: 'datalearnhome'
version: '1.0.0'
config-version: 2

profile: 'datalearnhome_dbt_project'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  datalearnhome:
    staging:
      +materialized: table
      # +schema: staging